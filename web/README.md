# TransiSafe Web

React and TypeScript interface for the TransiSafe Analysis API.

```sh
cd web
npm install
npm run dev
```

The Vite development server proxies `/api` to `http://localhost:8000`. For a
separate deployment, set `VITE_API_URL` to the public API origin before the
production build.
